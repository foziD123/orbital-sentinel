# Space-Track.org credentials.
# Sign up at https://www.space-track.org — free for research use.
# If either is unset, /api/live returns the cached fixture instead.
SPACETRACK_USER=
SPACETRACK_PASS=

# Cache TTL for live Space-Track fetches (seconds). 900 = 15 minutes.
LIVE_CACHE_TTL=900
